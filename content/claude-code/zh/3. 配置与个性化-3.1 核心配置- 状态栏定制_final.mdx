---
title: "Claude Code 状态栏定制完全指南：创建个性化信息面板"
description: "学习如何为 Claude Code 创建自定义状态栏，显示模型名称、目录、Git 分支和上下文使用情况等实时信息。"
date: "2026-02-10"
tool: "Claude Code"
slug: "status-bar-customization"
---

import AdPlaceholder from '@/components/AdPlaceholder'

# Claude Code 状态栏定制完全指南：创建个性化信息面板

状态栏是 Claude Code 界面底部的重要信息面板，类似于 shell 中的终端提示符。通过自定义状态栏，您可以实时查看当前模型、工作目录、Git 分支和资源使用情况等信息，让开发工作更加高效。

<AdPlaceholder />

## 前置条件

在学习状态栏定制之前，请确保您具备以下条件：

- **Claude Code**：已安装并可运行的 Claude Code
- **脚本基础**：了解基本的 Bash 或 Python 脚本编写
- **命令行工具**：熟悉终端操作和权限设置
- **jq 工具**：建议安装 jq 用于 JSON 解析

## 步骤 1：理解状态栏系统

### 步骤 1.1：状态栏概述

状态行显示在 Claude Code 界面的底部，提供实时上下文信息：

- **更新时机**：在对话消息更新时更新
- **更新频率**：最多每 300 毫秒运行一次
- **显示方式**：命令 stdout 的第一行成为状态栏文本
- **样式支持**：支持 ANSI 颜色代码设置样式

<ProTip>
状态栏可以帮助您快速了解当前会话状态，无需中断工作流程。
</ProTip>

### 步骤 1.2：配置方式

有两种方式配置状态栏：

**方式一：使用交互命令**
```bash
/statusline
```

Claude Code 会帮助您设置自定义状态栏，默认尝试重现终端提示符行为。

**方式二：手动配置**
```json
{
  "statusLine": {
    "type": "command",
    "command": "~/.claude/statusline.sh",
    "padding": 0
  }
}
```

<Note>
`padding` 参数可选，设置为 0 可让状态栏延伸到边缘。
</Note>

## 步骤 2：理解输入数据

### 步骤 2.1：JSON 输入结构

状态栏命令通过 stdin 接收 JSON 格式的会话上下文：

```json
{
  "hook_event_name": "Status",
  "session_id": "abc123...",
  "transcript_path": "/path/to/transcript.json",
  "cwd": "/current/working/directory",
  "model": {
    "id": "claude-opus-4-1",
    "display_name": "Opus"
  },
  "workspace": {
    "current_dir": "/current/working/directory",
    "project_dir": "/original/project/directory"
  },
  "version": "1.0.80",
  "output_style": {
    "name": "default"
  },
  "cost": {
    "total_cost_usd": 0.01234,
    "total_duration_ms": 45000,
    "total_api_duration_ms": 2300,
    "total_lines_added": 156,
    "total_lines_removed": 23
  },
  "context_window": {
    "total_input_tokens": 15234,
    "total_output_tokens": 4521,
    "context_window_size": 200000,
    "current_usage": {
      "input_tokens": 8500,
      "output_tokens": 1200,
      "cache_creation_input_tokens": 5000,
      "cache_read_input_tokens": 2000
    }
  }
}
```

### 步骤 2.2：关键数据字段

| 字段 | 描述 | 用途 |
|------|------|------|
| `model.display_name` | 模型显示名称 | 显示当前 AI 模型 |
| `workspace.current_dir` | 当前工作目录 | 显示位置信息 |
| `workspace.project_dir` | 项目根目录 | 区分项目 |
| `cost.total_cost_usd` | 累计成本 | 跟踪花费 |
| `cost.total_duration_ms` | 累计耗时 | 性能监控 |
| `context_window` | 上下文使用 | 资源监控 |

<AdPlaceholder />

## 步骤 3：创建基础状态栏

### 步骤 3.1：Bash 脚本模板

创建您的第一个状态栏脚本：

```bash
#!/bin/bash
# 从 stdin 读取 JSON 输入
input=$(cat)

# 使用 jq 提取值
MODEL_DISPLAY=$(echo "$input" | jq -r '.model.display_name')
CURRENT_DIR=$(echo "$input" | jq -r '.workspace.current_dir')

echo "[$MODEL_DISPLAY] 📁 ${CURRENT_DIR##*/}"
```

<ProTip>
`${CURRENT_DIR##*/}` 只显示当前目录名称，而不是完整路径，更加简洁。
</ProTip>

### 步骤 3.2：设置脚本权限

```bash
chmod +x ~/.claude/statusline.sh
```

### 步骤 3.3：配置状态栏

在 `settings.json` 中添加配置：

```json
{
  "statusLine": {
    "type": "command",
    "command": "~/.claude/statusline.sh"
  }
}
```

## 步骤 4：创建 Git 感知状态栏

### 步骤 4.1：显示 Git 分支

```bash
#!/bin/bash
input=$(cat)

MODEL_DISPLAY=$(echo "$input" | jq -r '.model.display_name')
CURRENT_DIR=$(echo "$input" | jq -r '.workspace.current_dir')

# 如果在 git 仓库中，显示 git 分支
GIT_BRANCH=""
if git rev-parse --git-dir > /dev/null 2>&1; then
    BRANCH=$(git branch --show-current 2>/dev/null)
    if [ -n "$BRANCH" ]; then
        GIT_BRANCH=" | 🌿 $BRANCH"
    fi
fi

echo "[$MODEL_DISPLAY] 📁 ${CURRENT_DIR##*/}$GIT_BRANCH"
```

<ProTip>
Git 分支信息对于多分支开发工作流特别有用。
</ProTip>

### 步骤 4.2：Python 版本

```python
#!/usr/bin/env python3
import json
import sys
import os

data = json.load(sys.stdin)

model = data['model']['display_name']
current_dir = os.path.basename(data['workspace']['current_dir'])

git_branch = ""
if os.path.exists('.git'):
    try:
        with open('.git/HEAD', 'r') as f:
            ref = f.read().strip()
            if ref.startswith('ref: refs/heads/'):
                git_branch = f" | 🌿 {ref.replace('ref: refs/heads/', '')}"
    except:
        pass

print(f"[{model}] 📁 {current_dir}{git_branch}")
```

### 步骤 4.3：Node.js 版本

```javascript
#!/usr/bin/env node

const fs = require('fs');
const path = require('path');

let input = '';
process.stdin.on('data', chunk => input += chunk);
process.stdin.on('end', () => {
    const data = JSON.parse(input);
    
    const model = data.model.display_name;
    const currentDir = path.basename(data.workspace.current_dir);
    
    let gitBranch = '';
    try {
        const headContent = fs.readFileSync('.git/HEAD', 'utf8').trim();
        if (headContent.startsWith('ref: refs/heads/')) {
            gitBranch = ` | 🌿 ${headContent.replace('ref: refs/heads/', '')}`;
        }
    } catch (e) {
        // 不是 git 仓库或无法读取 HEAD
    }
    
    console.log(`[${model}] 📁 ${currentDir}${gitBranch}`);
});
```

## 步骤 5：创建辅助函数

### 步骤 5.1：Bash 辅助函数模板

对于复杂的脚本，使用辅助函数简化数据提取：

```bash
#!/bin/bash
input=$(cat)

# 用于常见提取的辅助函数
get_model_name() { echo "$input" | jq -r '.model.display_name'; }
get_current_dir() { echo "$input" | jq -r '.workspace.current_dir'; }
get echo "$input"_project_dir() { | jq -r '.workspace.project_dir'; }
get_version() { echo "$input" | jq -r '.version'; }
get_cost() { echo "$input" | jq -r '.cost.total_cost_usd'; }
get_duration() { echo "$input" | jq -r '.cost.total_duration_ms'; }
get_lines_added() { echo "$input" | jq -r '.cost.total_lines_added'; }
get_lines_removed() { echo "$input" | jq -r '.cost.total_lines_removed'; }
get_input_tokens() { echo "$input" | jq -r '.context_window.total_input_tokens'; }
get_output_tokens() { echo "$input" | jq -r '.context_window.total_output_tokens'; }
get_context_window_size() { echo "$input" | jq -r '.context_window.context_window_size'; }

# 使用辅助函数
MODEL=$(get_model_name)
DIR=$(get_current_dir)
echo "[$MODEL] 📁 ${DIR##*/}"
```

<ProTip>
辅助函数让脚本更加模块化，便于维护和扩展。
</ProTip>

## 步骤 6：监控上下文使用

### 步骤 6.1：显示上下文百分比

```bash
#!/bin/bash
input=$(cat)

MODEL=$(echo "$input" | jq -r '.model.display_name')
CONTEXT_SIZE=$(echo "$input" | jq -r '.context_window.context_window_size')
USAGE=$(echo "$input" | jq '.context_window.current_usage')

if [ "$USAGE" != "null" ]; then
    CURRENT_TOKENS=$(echo "$USAGE" | jq '.input_tokens + .cache_creation_input_tokens + .cache_read_input_tokens')
    PERCENT_USED=$((CURRENT_TOKENS * 100 / CONTEXT_SIZE))
    echo "[$MODEL] Context: ${PERCENT_USED}%"
else
    echo "[$MODEL] Context: 0%"
fi
```

### 步骤 6.2：显示成本信息

```bash
#!/bin/bash
input=$(cat)

MODEL=$(echo "$input" | jq -r '.model.display_name')
COST=$(echo "$input" | jq -r '.cost.total_cost_usd')
DURATION=$(echo "$input" | jq -r '.cost.total_duration_ms')

echo "[$MODEL] 💰 \$$COST | ⏱️ ${DURATION}ms"
```

<ProTip>
成本和耗时监控对于管理项目预算和优化效率很有帮助。
</ProTip>

## 步骤 7：使用 Claude 生成状态栏

### 步骤 7.1：交互式生成

运行以下命令让 Claude Code 帮助生成状态栏：

```bash
/statusline show the model name in orange
```

### 步骤 7.2：自定义需求

可以向 Claude 提供具体说明：

```bash
/statusline show model, directory, and git branch in different colors
/statusline show context usage percentage and current cost
/statusline create a minimal status line with just model and branch
```

## 步骤 8：测试和调试

### 步骤 8.1：手动测试脚本

使用模拟 JSON 数据测试脚本：

```bash
echo '{"model":{"display_name":"Test"},"workspace":{"current_dir":"/test"}}' | ./statusline.sh
```

### 步骤 8.2：常见问题排查

| 问题 | 解决方案 |
|------|----------|
| 状态栏不显示 | 检查脚本是否可执行（chmod +x） |
| 脚本无输出 | 确保输出到 stdout（而不是 stderr） |
| JSON 解析失败 | 验证 jq 是否正确安装 |
| 颜色不生效 | 检查终端是否支持 ANSI 颜色 |

<ProTip>
保持状态栏简洁，只显示最重要的信息，以便快速扫描。
</ProTip>

## 专业提示

### 提示 1：保持简洁

状态栏应该适应一行显示：

```bash
# 好的示例
echo "[Opus] 📁 project | main"

# 不好的示例（太长）
echo "[Opus 4.6] 📁 /Users/user/projects/my-project/src/components | feature/new-auth-system"
```

<ProTip>
简洁的状态栏更易于快速扫描，不会分散注意力。
</ProTip>

### 提示 2：使用表情符号

使用表情符号使信息易于识别：

| 信息类型 | 表情符号 |
|----------|----------|
| 模型 | 🤖 |
| 目录 | 📁 |
| Git 分支 | 🌿 |
| 成本 | 💰 |
| 时间 | ⏱️ |
| 代码行 | 📝 |
| 上下文 | 🧠 |

### 提示 3：颜色编码

使用 ANSI 颜色代码进行视觉区分：

```bash
# 红色表示 Opus（复杂任务）
echo -e "\033[31m[Opus]\033[0m 📁 ${DIR##*/}"

# 绿色表示 Sonnet（日常任务）
echo -e "\033[32m[Sonnet]\033[0m 📁 ${DIR##*/}"
```

### 提示 4：性能优化

对于昂贵的操作（如 git 状态），考虑缓存：

```bash
#!/bin/bash
CACHE_FILE="/tmp/statusline_git_cache"

# 检查缓存是否有效（5秒内）
if [ -f "$CACHE_FILE" ] && [ $(( $(date +%s) - $(stat -c %Y "$CACHE_FILE") )) -lt 5 ]; then
    GIT_BRANCH=$(cat "$CACHE_FILE")
else
    GIT_BRANCH=$(git branch --show-current 2>/dev/null)
    echo "$GIT_BRANCH" > "$CACHE_FILE" 2>/dev/null
fi

echo "[Opus] 📁 ${PWD##*/} | 🌿 $GIT_BRANCH"
```

<ProTip>
缓存可以显著减少状态栏刷新的性能开销。
</ProTip>

## 常见问题排查

### 问题 1：脚本权限错误

**解决方案：**
```bash
chmod +x ~/.claude/statusline.sh
```

### 问题 2：jq 未安装

**解决方案（macOS）：**
```bash
brew install jq
```

**解决方案（Ubuntu/Debian）：**
```bash
sudo apt-get install jq
```

### 问题 3：输出格式问题

确保脚本输出纯文本，不包含其他字符：

```bash
# 正确
echo "[$MODEL] 📁 ${DIR##*/}"

# 错误（包含额外空格）
echo " [$MODEL] 📁 ${DIR##*/} "
```

### 问题 4：更新不及时

状态栏更新最多延迟 300 毫秒。如果需要更频繁的更新，考虑降低阈值（谨慎使用）。

## 结论

自定义状态栏是提升 Claude Code 使用体验的强大工具。通过本指南的学习，您已经了解了：

- ✅ 状态栏系统的基本原理
- ✅ 创建各种类型的状态栏脚本
- ✅ 提取和使用会话上下文数据
- ✅ 添加 Git 分支和成本监控
- ✅ 测试和调试技巧
- ✅ 性能优化方法

<ProTip>
一个精心设计的状态栏可以让您快速获取关键信息，提高开发效率。
</ProTip>

<AdPlaceholder />
